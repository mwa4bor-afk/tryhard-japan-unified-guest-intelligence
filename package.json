{
  "name": "tryhard-japan-unified-guest-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "Google Apps Script CRM and guest-intelligence platform for TryHard Japan.",
  "scripts": {
    "validate": "node scripts/validate-project.js",
    "login": "clasp login",
    "status": "clasp show-file-status",
    "pull": "clasp pull",
    "push": "npm run validate && clasp push",
    "version:create": "clasp create-version",
    "deploy:create": "npm run validate && clasp push && clasp create-deployment",
    "deploy:update": "npm run validate && clasp push && clasp update-deployment",
    "open": "clasp open-script"
  },
  "devDependencies": {
    "@google/clasp": "3.3.0"
  },
  "engines": {
    "node": ">=22"
  }
}
